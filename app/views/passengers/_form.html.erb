<%= simple_form_for(@passenger) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :user %>
    <%= f.association :ride %>
    <%= f.association :job %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
